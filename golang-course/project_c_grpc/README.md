# Project C: gRPC + REST Microservice Concept

This project demonstrates the usage of Protocol Buffers (`.proto`) and the conceptual layout of a dual gRPC and HTTP/JSON (grpc-gateway) microservice.

## Contents
- `user_service.proto`: The Protocol Buffers definition.
- `main.go`: The conceptual server layout.