package main

// TODO 1: Implement RemoveAtIndex
// It should return a new slice with the element at the specified index removed.
// If the index is out of bounds, return the original slice.
// Do this WITHOUT modifying the original slice (you must allocate a new slice).
func RemoveAtIndex(slice []int, index int) []int {
	return nil // Fix me
}

// TODO 2: Implement FilterMap
// It should take a map of string to int, and return a new map containing only
// the key-value pairs where the value is greater than or equal to the threshold.
func FilterMap(m map[string]int, threshold int) map[string]int {
	return nil // Fix me
}

// TODO 3: Implement CountFrequencies
// It should take a slice of strings and return a map where the keys are the
// unique strings and the values are the number of times they appeared in the slice.
func CountFrequencies(words []string) map[string]int {
	return nil // Fix me
}

// --- TODO 4: Structs & Methods ---

// Define a struct named `Rectangle` with fields `Width` and `Height` (both float64).
type Rectangle struct {
	// Fix me
}

// Define a method `Area()` on `Rectangle` that returns its area.
// func (r Rectangle) Area() float64 { ... }

// Define a method `Scale(factor float64)` on `Rectangle` that multiplies
// both width and height by the given factor. It MUST modify the original rectangle.
// func (r *Rectangle) Scale(factor float64) { ... }
